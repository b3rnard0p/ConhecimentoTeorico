version: '3.8'

services:
  pycaret-env:
    build: .
    container_name: pycaret_class_container
    volumes:
      - .:/app
    ports:
      - "8888:8888"
    command: jupyter notebook --ip=0.0.0.0 --port=8888 --no-browser --allow-root




    